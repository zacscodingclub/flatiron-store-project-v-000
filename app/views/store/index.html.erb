<% @categories.each do |category| %>
  <%= link_to category.name, category_path(category) %>
<% end %>

<% @items.each do |item| %>
  <p><%= item.title %> | <%= to_dollars(item.price) %> | <%= button_to 'Add to Cart', line_items_path(item_id: item) %></p>
<% end %>
